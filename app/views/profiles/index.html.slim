#page
  .control.is-grouped
    .control
      a.button data-bind="click: add" Add
  <!-- ko foreach: chunks -->
  .columns data-bind="foreach: $data"
    .column.is-3
      = render('card')
  <!-- /ko -->
  
  = render('modal_profile')
  = render('modal_invite')

= content_for(:javascript) do
  javascript:
    var profiles = #{raw MultiJson.encode(@profiles)};
    var user = #{raw MultiJson.encode(@user)};
